/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // next.config.js

  images: {
    domains: ['images.unsplash.com'], // Add other domains here as needed
  },


};

export default nextConfig;
